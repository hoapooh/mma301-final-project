import { View, Text, SafeAreaView } from 'react-native';
import React from 'react';

const ScreenLoader = () => {
  return (
    <SafeAreaView
      style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}
    >
      <Text style={{ fontSize: 40 }}>Logo App o day di </Text>
    </SafeAreaView>
  );
};

export default ScreenLoader;
